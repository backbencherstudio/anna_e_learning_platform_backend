<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= subject || 'Notification' %></title>
    <style>
      body { font-family: Arial, Helvetica, sans-serif; background: #f6f7fb; margin: 0; padding: 24px; }
      .container { max-width: 640px; margin: 0 auto; background: #ffffff; border-radius: 8px; box-shadow: 0 2px 8px rgba(0,0,0,0.06); overflow: hidden; }
      .header { background: #0d6efd; color: #ffffff; padding: 16px 20px; font-size: 18px; font-weight: 600; }
      .content { padding: 20px; color: #202124; line-height: 1.6; }
      .footer { padding: 16px 20px; color: #6b7280; font-size: 12px; }
      .btn { display: inline-block; padding: 10px 16px; background: #0d6efd; color: #fff; border-radius: 6px; text-decoration: none; }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <%= appName || 'Our Platform' %>
      </div>
      <div class="content">
        <% if (name) { %>
          <p>Hi <strong><%= name %></strong>,</p>
        <% } else { %>
          <p>Hi,</p>
        <% } %>

        <p><%= message %></p>

        <p style="margin-top: 24px;">Regards,<br/><strong><%= appName || 'Our Platform' %></strong></p>
      </div>
      <div class="footer">
        This email was sent automatically by <%= appName || 'Our Platform' %>. Please do not reply.
      </div>
    </div>
  </body>
  </html>


