<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>You received a card!</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            text-align: center;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 30px;
            border-radius: 10px;
            margin-bottom: 30px;
        }
        .card-container {
            border: 2px solid #ddd;
            border-radius: 15px;
            padding: 30px;
            margin: 20px 0;
            background: #f9f9f9;
            text-align: center;
        }
        .card-image {
            max-width: 100%;
            height: auto;
            border-radius: 10px;
            margin-bottom: 20px;
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        }
        .card-title {
            font-size: 24px;
            font-weight: bold;
            color: #333;
            margin-bottom: 15px;
        }
        .card-message {
            font-size: 16px;
            color: #666;
            margin-bottom: 20px;
            line-height: 1.8;
        }
        .sender-info {
            background: #e8f4fd;
            padding: 15px;
            border-radius: 8px;
            margin-top: 20px;
        }
        .footer {
            text-align: center;
            margin-top: 30px;
            padding: 20px;
            background: #f5f5f5;
            border-radius: 8px;
            color: #666;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>🎉 You received a special card!</h1>
        <p>Someone has sent you a personalized card</p>
    </div>

    <div class="card-container">
        <% if (cardImageUrl) { %>
            <img src="<%= cardImageUrl %>" alt="<%= cardGenerator.title %>" class="card-image">
        <% } %>
        
        <div class="card-title"><%= cardGenerator.title %></div>
        
        <div class="card-message">
            <%= cardGenerator.message %>
        </div>

        <% if (cardGenerator.sender_name) { %>
            <div class="sender-info">
                <strong>From:</strong> <%= cardGenerator.sender_name %>
            </div>
        <% } %>
    </div>

    <% if (recipientName) { %>
        <p>Dear <%= recipientName %>,</p>
    <% } %>

    <p>You have received this beautiful card from our e-learning platform. We hope it brings a smile to your face!</p>

    <div class="footer">
        <p>This card was generated through our e-learning platform.</p>
        <p>Thank you for being part of our learning community!</p>
    </div>
</body>
</html>
